# Git LFS Configuration
# Track only large binary files in examples/ directory

# 3D velocity fields (very large: 50-200 MB each)
examples/**/*.vti filter=lfs diff=lfs merge=lfs -text
examples/**/*.h5 filter=lfs diff=lfs merge=lfs -text

# Binary structure function files (large)
examples/**/*.bin filter=lfs diff=lfs merge=lfs -text

