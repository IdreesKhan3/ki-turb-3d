# Git LFS Configuration
# Track large files in example data directory

# Track all files in examples/showcase/ directory
examples/showcase/** filter=lfs diff=lfs merge=lfs -text

# Track VTI files (3D velocity fields - very large)
*.vti filter=lfs diff=lfs merge=lfs -text

# Track binary structure function files (can be large)
*.bin filter=lfs diff=lfs merge=lfs -text

# Optional: Track large CSV files if needed
# *.csv filter=lfs diff=lfs merge=lfs -text

